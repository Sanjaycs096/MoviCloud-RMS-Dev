{
  "name": "movicloud-rms",
  "version": "1.0.0",
  "private": true,
  "description": "MoviCloud RMS – Python backend. See Procfile for the correct start command.",
  "scripts": {
    "build": "echo 'Python service: see Procfile and render.yaml for build/start'",
    "start": "bash start.sh",
    "dev": "npm --prefix User_side run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
